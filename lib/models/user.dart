class MyUser {
  String uid, email, number, role;
  MyUser({this.email, this.uid, this.number, this.role});
}

class UserData {
  String uid;
  String username;
  String email;
  UserData({this.uid, this.username, this.email});
}
